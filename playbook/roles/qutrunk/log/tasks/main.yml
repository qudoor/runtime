- name: "{{ task_name }}"
  kubernetes.core.k8s_log:
    kubeconfig: "{{ kubeconfig_file }}"
    namespace: "{{ kubernetes_qutrunk_namespace }}"
    label_selectors:
      - job-name = qutrunk-{{ qutrunk_id }}